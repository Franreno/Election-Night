# Host port mappings — change if defaults conflict with existing services
FRONTEND_PORT=3000
BACKEND_PORT=8000
DB_PORT=5432

# Backend (FastAPI) — see backend/app/config.py
DATABASE_URL=postgresql://postgres:postgres@db:5432/election
CORS_ORIGINS=["http://localhost:3000"]
MAX_UPLOAD_SIZE_BYTES=104857600  # 100 MB

# Frontend (Next.js) — used at build time
NEXT_PUBLIC_API_URL=http://localhost:8000
